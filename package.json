{
  "name": "react-admin-panel-lerna",
  "version": "0.1.0",
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "react",
      "**/react",
      "react/**",
      "**/react-*",
      "react-*/**",
      "**/*-react",
      "*-react/**"
    ]
  },
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@types/jest": "^24.0.0",
    "@types/node": "^12.0.0",
    "@types/styled-components": "4.1.1",
    "lerna": "^3.22.1",
    "lodash": "4.17.19",
    "node-fetch": "2.6.1",
    "styled-components": "4.1.1"
  },
  "scripts": {
    "watch": "lerna run --parallel watch",
    "build:rap-ui": "yarn workspace @rap/ui build",
    "start:rap-ui": "yarn workspace @rap/ui watch",
    "build:rap-core": "yarn workspace @rap/core build",
    "start:rap-core": "yarn workspace @rap/core build",
    "build:rap-main": "yarn workspace @rap/main build",
    "start:rap-main": "yarn workspace @rap/main build",
    "pre:build": "yarn build:rap-ui && yarn build:rap-core && yarn build:rap-main && yarn lerna link",
    "start:demo": "yarn workspace @rap/demo cross-env REACT_APP_DATA_PROVIDER=rest yarn -s start",
    "build:demo": "yarn pre:build && yarn workspace @rap/demo build",
    "dev-build:demo": "yarn pre:build && yarn workspace @rap/demo dev-build",
    "run:demo-watch": "concurrently \"yarn watch\" \"yarn start:demo\"",
    "test:rap-demo": "yarn workspace @rap-demo test",
    "prettier": "prettier"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "concurrently": "^5.2.0",
    "cross-env": "^7.0.2",
    "lerna": "^3.22.1",
    "prettier": "^2.0.5"
  }
}
