{
  "name": "react-admin-panel-lerna",
  "version": "0.1.0",
  "private": true,
  "dependencies": {
    "@testing-library/jest-dom": "^4.2.4",
    "@types/jest": "^24.0.0",
    "@types/node": "^12.0.0",
    "typescript": "~3.7.2"
  },
  "scripts": {
    "run-demo": "cd examples/demo && yarn start",
    "test": "react-scripts test",
    "prettier": "prettier"
  },
  "eslintConfig": {
    "extends": "react-app"
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "lerna": "^3.22.1",
    "prettier": "^2.0.5"
  }
}
